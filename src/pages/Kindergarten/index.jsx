
import { Container} from "@mui/material";

export default () => {
    /* TODO: react-query vs axios */
    // if axios we should use effect of react
    return <Container>
        Kindergarten
    </Container>
}
